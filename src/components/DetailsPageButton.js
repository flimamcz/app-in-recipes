import React from 'react';
// import PropTypes from 'prop-types';

function DetailsPageButton() {
  return (
    <button
      type="button"
      data-testid="start-recipe-btn"
    >
      Start Recipe
    </button>
  );
}

// NomeDoComponente.propTypes = { key: PropTypes.type};

export default DetailsPageButton;
