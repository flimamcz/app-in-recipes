import React from 'react';
// import PropTypes from 'prop-types';

function Loading() {
  return (
    <h3>Carregando...</h3>
  );
}

// Loading.propTypes = { key: PropTypes.type};

export default Loading;
