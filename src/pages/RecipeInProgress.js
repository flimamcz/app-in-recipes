import React from 'react';
// import PropTypes from 'prop-types';

function RecipeInProgress() {
  return (
    <h3>RecipeProgress</h3>
  );
}

// RecipeInProgress.propTypes = { key: PropTypes.type};

export default RecipeInProgress;
