import React from 'react';
import Header from '../components/Header';
// import PropTypes from 'prop-types';

function Meals() {
  return (
    <div>
      <Header title="Meals" searchImage />
    </div>
  );
}

// Meals.propTypes = { key: PropTypes.type};

export default Meals;
