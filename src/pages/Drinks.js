import React from 'react';
import Header from '../components/Header';
// import PropTypes from 'prop-types';

function Drinks() {
  return (
    <Header title="Drinks" />
  );
}

// Drinks.propTypes = { key: PropTypes.type};

export default Drinks;
