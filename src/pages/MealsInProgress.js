import React from 'react';
// import PropTypes from 'prop-types';

function MealsInProgress() {
  return (
    <h3>MealsInProgress</h3>
  );
}

// MealsInProgress.propTypes = { key: PropTypes.type};

export default MealsInProgress;
