import React from 'react';
// import PropTypes from 'prop-types';

function RecipeDetails() {
  return (
    <h3>RecipeDetails</h3>
  );
}

// RecipeDetails.propTypes = { key: PropTypes.type};

export default RecipeDetails;
